# ColorCounter

colorStats.py takes an image and returns an HTML page listing the most used colors by pixel.
-Ignores colors too close to black or white

Color.py is an object class which allows for easy storage, manipulation, and conversion of color values
